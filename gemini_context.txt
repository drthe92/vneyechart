# KIẾN TRÚC DỰ ÁN WEB: vision-therapy-webapp

## 1. SƠ ĐỒ THƯ MỤC
```text
📂 vision-therapy-webapp/
    📄 index.html
    📄 sw.js                       (🔥 MỚI: Service Worker — PWA offline, Network First)
    📄 manifest.json               (🔥 MỚI: Web App Manifest — cài đặt ứng dụng)
    📄 offline.html                (🔥 MỚI: trang fallback khi điều hướng ngoại tuyến)
    📂 icons/                      (🔥 MỚI: icon PWA placeholder 192/512)
    📂 js/
        📄 settings.js
        📄 exam_session_manager.js
        📄 controller.js
        📄 credit_card_calibration.js
        📄 calibration.js
        📄 main.js
        📄 firebase_config.js          (🔥 MỚI: khởi tạo Firebase Firestore → window.db)
        📄 dashboard_controller.js     (🔥 MỚI: Biểu đồ tiến triển, Chart.js + Firestore)
    📂 docs/
        📄 index.html                  (🔥 MỚI: trung tâm tài liệu y khoa)
        📄 css/docs.css
        📄 m01_catch.html … m12_pursuit.html   (🔥 MỚI: 12 trang tài liệu từng module)
        📄 post_op_strabismus_protocol.html    (🔥 MỚI: Phác đồ hậu phẫu lác)
    📂 neuro_ophthalmology/
        📄 main.py
        📄 calibration.py
        📄 ui.py
        📄 __init__.py
        📄 red_desat.py
        📄 okn_test.py
        📄 warning_dialog.py
    📂 css/
        📄 exam_session.css
        📄 style_new.css
        📄 credit_card_calibration.css
        📄 style.css
    📂 modules/
        📄 near_lea.js
        📄 hotv.js
        📄 number_chart.js
        📄 retina_subs.js
        📄 auto_BCVA_tumbling_e_for_amblyopia.js
        📄 therapeutic_menu_controller.js
        📄 schober_test.js
        📄 astigmatism.js
        📄 stereo_anaglyph.js
        📄 neuro_okn.js
        📄 crosstalk.js
        📄 vergence_tracker_game.js
        📄 anti_suppression_catch.js
        📄 rds_therapy_game.js
        📄 red_desat.js
        📄 retina_ishihara.js
        📄 shape_alignment.js
        📄 tumbling_e.js
        📄 auckland_logmar.js
        📄 near_npoint.js
        📄 saccadic_tracking_game.js
        📄 landolt_c.js
        📄 hiding_heidi.js
        📄 astigmatism_jcc.js
        📄 duochrome_test.js
        📄 etdrs_chart.js
        📄 dynamic_fixation.js
        📄 binocular_game_engine.js
        📄 dynamic_vergence.js
        📄 maddox_grid_module.js
        📄 drthe_optotype_loader.js
        📄 optotype_paths.js
        📄 jcc_simulation.js
        📄 retina_amsler.js
        📄 retina_pelli_robson.js
        📄 near_logmar.js
        📄 distance_va.js
        📄 lea_symbols.js
        📄 divergence_therapy_game.js
        📄 snellen_chart.js
        📄 worth4dot.js
        📄 cam_visual_stimulator_game.js      (🔥 MỚI: M7 CAM)
        📄 anti_crowding_game.js              (🔥 MỚI: M8 Anti-Crowding)
        📄 red_cone_stimulator_game.js        (🔥 MỚI: M9 RED-Cone)
        📄 okn_stimulation_game.js            (🔥 MỚI: M10 OKN Tracker)
        📄 gabor_perceptual_learning_game.js  (🔥 MỚI: M11 Gabor PL)
        📄 dichoptic_pursuit_game.js          (🔥 MỚI: M12 Pursuit)
        📄 convergence_therapy_game.js        (🔥 MỚI: M13 Hội tụ)
    📂 generated/
        📂 drthe_optotype/
            📂 sloan/
            📂 svg/
            📂 Auckland/
            📂 numbers/
        📂 old_generated/
        📂 new_generated/
        📂 ishihara_color_test/
```

## 2. GIAO DIỆN MÃ NGUỒN LÕI

### File: `index.html`
```html
// DOM IDs có sẵn để JS tương tác:
IDs = ['app', 'sidebar', 'sidebar-header', 'settings-btn', 'cc-calib-btn', 'fullscreen-btn', 'sidebar-title', 'workspace-toggle-btn', 'menu-diagnostic', 'menu-therapeutic', 'menu-toggle-btn', 'menu-hotzone', 'main-area', 'workspace-diagnostic', 'display-board', 'workspace-therapeutic', 'therapeutic-content']

// PWA (🔥 MỚI):
// <head>: <link rel="manifest" href="/manifest.json"> + meta theme-color #1a1a2e
// </body>: đăng ký navigator.serviceWorker.register('/sw.js') khi window load
```

### File: `js/calibration.js`
```javascript
function getOptotypeSize(logmarValue, calib = null): ...
function _loadCalibFromStorage(): ...
function _estimatePPI(): ...
class DisplayCalibrator:
const closeModal = () => ...
```

### File: `js/controller.js`
```javascript
function throttle(fn, cooldown): ...
class UniversalInput:
```

### File: `js/credit_card_calibration.js`
```javascript
class CreditCardCalibrator:
```

### File: `js/dashboard_controller.js`
```javascript
window.openDashboard = () => ...
window.closeDashboard = () => ...
window.renderChart = () => ...
function fetchFirebaseData(): ...
function buildModuleDropdown(): ...
window.renderDashboard = (moduleKey) => ...
function _renderPrismChart(cfg, seriesList, groupLabel): ...
function _renderLevelChart(cfg, seriesList, groupLabel): ...
function _renderScoreChart(cfg, seriesList, groupLabel): ...
function _baseOptions(yTitle, beginAtZero): ...
function _levelOptions(): ...
function _createChartCard(title): ...
function _showEmpty(message): ...
function _destroyCharts(): ...
function _getSelectedModuleId(): ...
function _getDateRange(): ...
function _filterSeriesByRange(series, fromMs, toMs): ...
function _buildTimeline(seriesList): ...
function _moduleNumberFromGameName(gName): ...
function _fmtNum(v): ...
function _fmtDuration(sec): ...
```

### File: `js/exam_session_manager.js`
```javascript
function normalizeString(str): ...
function buildPatientId(phone, name, yob): ...
function handleSessionStart(event): ...
function init(): ...
function loadColorCalibration(): ...
function createUI(): ...
function createStartExamModal(): ...
function createEndExamModal(): ...
function createReportModal(): ...
function createPrintContainer(): ...
function formatClinicalMetrics(metrics): ...
window.refreshTestViews = () => ...
function generateReportHTML(isPrintMode, examData = null): ...
function saveSession(): ...
function restoreSession(): ...
function createToastContainer(): ...
function createManualSaveFab(): ...
function createManualEntryModal(): ...
function handleManualSaveFabClick(): ...
function tryAutoDetectAndSave(): ...
function handleManualEntrySubmit(e): ...
function resetManualEntryForm(): ...
function bindEvents(): ...
function setupVisionTestListener(): ...
function setupGlobalHotkey(): ...
function cleanupGlobalHotkey(): ...
function startExam(patientName, patientYOB, patientId): ...
function updateExamUI(): ...
function showReportModal(examData, isFromHistory = false): ...
function printReport(): ...
window.onafterprint = () => ...
function exportPDF(examData = null): ...
function saveToHistory(exam): ...
function resetSession(): ...
function enterFullscreen(): ...
function showModal(modal): ...
function hideModal(modal): ...
function prefillStartExamModal(): ...   // 🔥 điền sẵn Năm sinh từ localStorage.currentPatientYob khi mở modal
function showToast(message): ...
function formatHistoryDate(timestamp): ...
function createHistoryViewerUI(): ...
function openHistoryModal(): ...
function renderHistoryTable(searchTerm = ''): ...
function exportHistoryToCSV(): ...
function escapeCell(value): ...
function allowTypingInModal(modalElement): ...
const stopGlobalHotkeys = (e) => ...
const handleCompositionStart = (e) => ...
const handleCompositionEnd = (e) => ...
function compressImage(file, maxWidth, quality, callback): ...
function loadClinicSettings(): ...
function saveClinicSettings(): ...
function createCalibrationSection(): ...
const createPalette = (type, titleStr, colorArray) => ...
function createClinicSettingsUI(): ...
function openClinicSettingsModal(): ...
const renderDisplayPresetButtons = () => ...
const applyBtnStyle = (selected) => ...
const renderPalettes = (type, colorArray, containerClass) => ...
function generateClinicHeaderHTML(): ...
function escapeHtml(text): ...
```

### File: `js/firebase_config.js`
```javascript
```

### File: `js/main.js`
```javascript
function loadTest(testId, steps): ...
function nextStep(): ...
function prevStep(): ...
function shuffleStep(): ...
function back(): ...
function registerTestModule(mod): ...
function getTestModule(id): ...
const _getMaddoxCalibration = () => ...
function makeModule(id, label, steps = DEFAULT_STEPS): ...
function renderStep(): ...
function highlightMenuItem(testId): ...
function setupSidebar(): ...
function toggleSidebar(forceShow): ...
const getMenuItems = () => ...
function updateMenuFocus(): ...
function isMenuVisible(): ...
function findClosestElementInDirection(currentIndex, direction, items): ...
const verticalOverlap = (a, b) => ...
function restoreFocusAfterModalClose(): ...
function isModalVisible(modal): ...
function isFullscreenOverlay(element): ...
function getModalCandidates(): ...
const add = (element) => ...
function getActiveModal(): ...
function getModalContentElement(modal): ...
function isVisibleFocusableElement(element): ...
function isTextInput(el): ...
function getFocusableItems(modal): ...
function focusModalItem(items, index): ...
function closeActiveModal(modal): ...
function getModalCloseButton(modal): ...
function handleModalUXKeydown(e, modal): ...
window.enhanceModalUX = () => ...
function handleModalKeydown(e, modal): ...
function initModalAutoFocus(): ...
const enhanceActiveModal = () => ...
const schedule = () => ...
function setupInput(): ...
function setupDisplay(): ...
function setupSettingsButton(): ...
function setupCalibrator(): ...
function setupCCButton(): ...
function toggleWorkspace(): ...
function setupWorkspaceToggle(): ...
function init(): ...
function formatTherapyClinicalResult(input): ...
function generateTherapyReportHTML(patientId): ...
// (initPWA IIFE cuối file — 🔥 MỚI: SW register + toast sẵn sàng/update + badge offline)
```

### File: `js/settings.js`
```javascript
class DisplayManager:
```

### File: `neuro_ophthalmology/__init__.py`
```python
```

### File: `neuro_ophthalmology/calibration.py`
```python
class ScreenInfo:
```

### File: `neuro_ophthalmology/main.py`
```python
def main(): ...
```

### File: `neuro_ophthalmology/okn_test.py`
```python
class OKNTest:
```

### File: `neuro_ophthalmology/red_desat.py`
```python
def hsv_to_rgb_int(h_deg, s_pct, v_pct): ...
def rgb_to_css(r, g, b): ...
class Button:
class Slider:
class RedDesaturationApp:
```

### File: `neuro_ophthalmology/ui.py`
```python
class InputField:
class Button:
class MainMenu:
```

### File: `neuro_ophthalmology/warning_dialog.py`
```python
class WarningDialog:
```

### File: `modules/anti_crowding_game.js`
```javascript
class AntiCrowdingGame:
```

### File: `modules/anti_suppression_catch.js`
```javascript
class CatchGame:
```

### File: `modules/astigmatism.js`
```javascript
function drawDial(ctx, width, height, dpr, rotation, isDuochrome): ...
function drawMaddoxIndicator(ctx, cx, cy, radius, rotation, baseLineWidth): ...
```

### File: `modules/astigmatism_jcc.js`
```javascript
class JCCRenderer:
```

### File: `modules/auckland_logmar.js`
```javascript
function shuffle(arr): ...
function pickN(n): ...
function logmarToSnellen(logmar): ...
function loadAuckland(name): ...
const buildRow = (n) => ...
```

### File: `modules/auto_BCVA_tumbling_e_for_amblyopia.js`
```javascript
function shuffle(arr): ...
function roundLogmar(v): ...
function rotateE(orientation): ...
function crowdingBars(): ...
function formatLogmar(val): ...
function parseLogmar(val): ...
function buildConclusion(p1, p2, diff): ...
```

### File: `modules/binocular_game_engine.js`
```javascript
class BinocularGameEngine:
```

### File: `modules/cam_visual_stimulator_game.js`
```javascript
class CamVisualStimulatorGame:
```

### File: `modules/convergence_therapy_game.js`
```javascript
class ConvergenceTherapyGame:
```

### File: `modules/crosstalk.js`
```javascript
function compensate(r, g, b, ct = CROSSTALK): ...
const cl = (v) => ...
const hx = (v) => ...
```

### File: `modules/dichoptic_pursuit_game.js`
```javascript
class DichopticPursuitGame:
```

### File: `modules/distance_va.js`
```javascript
function shuffle(arr): ...
function pickRandomLetters(): ...
function logmarToSnellen(logmar): ...
```

### File: `modules/divergence_therapy_game.js`
```javascript
class DivergenceTherapyGame:
```

### File: `modules/drthe_optotype_loader.js`
```javascript
function extractOptotypePath(svgText): ...
function loadOptotype(group, name): ...
function loadMany(specs): ...
```

### File: `modules/duochrome_test.js`
```javascript
```

### File: `modules/dynamic_fixation.js`
```javascript
const animate = () => ...
```

### File: `modules/dynamic_vergence.js`
```javascript
function getDynamicVergenceRightEyeColor(): ...
function getDynamicVergenceLeftEyeColor(): ...
```

### File: `modules/etdrs_chart.js`
```javascript
function shuffle(arr): ...
function pick5(): ...
const buildRow = (n) => ...
```

### File: `modules/gabor_perceptual_learning_game.js`
```javascript
class GaborPerceptualLearningGame:
const beep = (freq, startOffset) => ...
```

### File: `modules/hiding_heidi.js`
```javascript
```

### File: `modules/hotv.js`
```javascript
function shuffle(arr): ...
function pickLetters(): ...
```

### File: `modules/jcc_simulation.js`
```javascript
function createShader(gl, type, source): ...
function createProgram(gl, vertexShader, fragmentShader): ...
function createTextureFromCanvas(gl, canvas): ...
function createLandoltCTexture(size = 512): ...
function createAucklandOptotypeTexture(size = 512): ...
```

### File: `modules/landolt_c.js`
```javascript
function shuffle(arr): ...
function rotate(path, orientation): ...
```

### File: `modules/lea_symbols.js`
```javascript
function shuffle(arr): ...
function pickSymbols(): ...
```

### File: `modules/maddox_grid_module.js`
```javascript
class MaddoxGridModule:
```

### File: `modules/near_lea.js`
```javascript
function shuffle(arr): ...
function pick4(): ...
function getCalibration(): ...
function mmToPx(heightMm, pxPerMm): ...
function buildFullChart(highlightIndex, symbolsMatrix, calib): ...
function buildSingleRow(rowIndex, symbols, calib): ...
function buildSingleOptotype(rowIndex, symbolKey, showBars, calib): ...
function buildInfoPanel(logmar, rowIndex, totalRows, mode): ...
function buildToolbar(mode, showCrowdingBars): ...
```

### File: `modules/near_logmar.js`
```javascript
function shuffle(arr): ...
function pick5(): ...
function getCalibration(): ...
function mmToPx(heightMm, pxPerMm): ...
function renderCrowdingBars(pxSize): ...
function buildFullChart(highlightIndex, lettersMatrix, calib): ...
function buildSingleRow(rowIndex, letters, calib): ...
function buildSingleOptotype(rowIndex, letter, showBars, calib): ...
function buildInfoPanel(logmar, rowIndex, totalRows, mode): ...
function buildToolbar(mode, showCrowdingBars): ...
```

### File: `modules/near_npoint.js`
```javascript
function shuffle(arr): ...
function getCalibration(): ...
function mmToPx(heightMm, pxPerMm): ...
function pickParagraph(history): ...
```

### File: `modules/neuro_okn.js`
```javascript
function calculatePPD(distanceM, ppi): ...
function getStripeWidth(ppd, cpd): ...
function showWarningDialog(onConfirm): ...
class WebGLOKNRenderer:
const updatePreview = () => ...
```

### File: `modules/number_chart.js`
```javascript
function shuffle(arr): ...
function pickDigits(n): ...
function state_stepIndex(): ...
```

### File: `modules/okn_stimulation_game.js`
```javascript
class OKNStimulationGame:
```

### File: `modules/optotype_paths.js`
```javascript
function renderOptotype(pathStr, pxSize): ...
function renderOptotypeRow(pathArray, pxSize, gap = 0): ...
```

### File: `modules/rds_therapy_game.js`
```javascript
class RDSTherapyGame:
```

### File: `modules/red_cone_stimulator_game.js`
```javascript
class RedConeStimulatorGame:
```

### File: `modules/red_desat.js`
```javascript
function hsvToRgb(h, s, v): ...
function rgbToCss(r, g, b): ...
```

### File: `modules/retina_amsler.js`
```javascript
```

### File: `modules/retina_ishihara.js`
```javascript
```

### File: `modules/retina_pelli_robson.js`
```javascript
function shuffle(arr): ...
function pickLetters(n): ...
function logcsToGray(logcs): ...
function sloanSvg(letter, pxSize, gray): ...
function calculateDitheredGray(logcs): ...
function sloanDitheredSvg(letter, pxSize, logcs): ...
const prev = () => ...
const next = () => ...
```

### File: `modules/retina_subs.js`
```javascript
function shuffle(arr): ...
function pickLetters(n): ...
function logcsToGray(logcs): ...
function sloanSvg(letter, pxSize, gray): ...
function calculateDitheredGray(logcs): ...
function sloanDitheredSvg(letter, pxSize, logcs): ...
const prev = () => ...
const next = () => ...
```

### File: `modules/saccadic_tracking_game.js`
```javascript
class SaccadicTrackingGame:
```

### File: `modules/schober_test.js`
```javascript
function getTargetRightEyeColor(): ...
function getTargetLeftEyeColor(): ...
```

### File: `modules/shape_alignment.js`
```javascript
class ShapeAlignmentGame:
```

### File: `modules/snellen_chart.js`
```javascript
function shuffle(arr): ...
function pickLetters(n): ...
function state_stepIndex(): ...
```

### File: `modules/stereo_anaglyph.js`
```javascript
class StereoRenderer:
```

### File: `modules/therapeutic_menu_controller.js`
```javascript
class TherapeuticMenuController:
const closeLobbyHandler = () => ...
window.startTherapyModule = (id) => ...
window.renderTherapeuticLobby = (container) => ...
window.refreshTherapeuticMenu = () => ...
window.syncM12ProgressFromFirebase = (patientId, moduleKey = 'M12') => ...
window.closeTherapyModule = () => ...
```

### File: `modules/tumbling_e.js`
```javascript
function shuffle(arr): ...
function rotate(path, orientation): ...
```

### File: `modules/vergence_tracker_game.js`
```javascript
class VergenceTrackerGame:
```

### File: `modules/worth4dot.js`
```javascript
function getTargetRedEyeColor(): ...
function getTargetGreenEyeColor(): ...
const drawShape = (type, cx, cy, r, fill, opacity) => ...
```


## 3. CÔNG NGHỆ & TỔNG QUAN HỆ THỐNG

- **Frontend**: Vanilla JavaScript (ES6 Modules, `import`/`export`),
  HTML5 Canvas 2D, WebGL, SVG. KHÔNG dùng framework (React/Vue).
- **Backend đám mây**: Firebase (SDK compat 9.23.0, load qua CDN trong
  index.html) — `js/firebase_config.js` khởi tạo `window.db` (Firestore).
  Lưu hồ sơ bệnh nhân + phiên tập; đồng bộ song song với localStorage.
- **Biểu đồ**: Chart.js (CDN) qua `js/dashboard_controller.js` — biểu đồ
  tiến triển chỉ số lâm sàng theo phiên, dữ liệu đọc từ Firestore.
- **State**: object `state` (js/main.js) — `currentTest`, `steps`,
  `stepIndex`, `history` (stack BACK). Không có store trung tâm;
  truyền qua biến module + `window.__*`.
- **Storage**: `localStorage` (offline-first), tối đa 200 bản ghi EMR +
  **Firestore** (Patients/{patientId}/Sessions) khi có mạng.
- **Backend y khoa phụ trợ**: `neuro_ophthalmology/` là app **Python +
  Pygame** chạy độc lập (OKN, Red Desaturation, menu cấu hình màn hình),
  không giao tiếp trực tiếp với webapp JS — tách biệt hoàn toàn.
- **Kiến trúc**: Module-based, dynamic registry, Global Event Delegation.
  Webapp là SPA đo thị lực + Mini-EMR + Phòng tập thị giác (13 game)
  cho phòng khám nhãn khoa.

## 4. HỢP ĐỒNG MODULE BÀI TEST (DIAGNOSTIC)

Mọi bài test thị lực là một object đăng ký qua `registerTestModule(mod)`:

```javascript
const mod = {
  id: 'far-vision-tumbling-e',   // phải khớp data-test trong index.html
  label: 'Tumbling E',
  steps: [1.0, 0.9, ..., -0.3],  // mảng bước (thường là LogMAR levels)
  _state: ...,                    // trường nội bộ tuỳ ý

  render(index) {                 // BẮT BUỘC: vẽ bước thứ index vào #display-board
    const logmar = this.steps[index];
    const pxSize = getOptotypeSize(logmar, calib);
    document.getElementById('display-board').innerHTML = `...svg...`;
  },

  cleanup()  { /* TÙY CHỌN: dọn loop (vd OKN/Maddox) khi chuyển test */ },
  randomize() { /* TÙY CHỌN: xáo lại ký tự, GIỮ nguyên LogMAR */ },
};
registerTestModule(mod);
```

- `loadTest(testId, steps)`: gọi `prevMod.cleanup()`, push history,
  reset `stepIndex=0`, `renderStep()`. Luôn `universalInput.resume()`.
- `renderStep()`: gọi `mod.render(state.stepIndex)` vào `#display-board`.
- Điều hướng: ArrowRight/Down/Enter/PageDown → NEXT (index++);
  ArrowLeft/Up/PageUp → PREV; Escape/Backspace → BACK (pop history);
  Space → SHUFFLE (gọi `mod.randomize()` hoặc `renderStep()`).
- Sidebar click: nếu `id.startsWith('near-vision-')` → `applyNearVisionPreset()`
  (0.4m); `far-vision-` → `applyDistanceVisionPreset()` (4m).

## 5. HỆ THỐNG INPUT CHUẨN (UniversalInput)

`js/controller.js` — phát `CustomEvent` lên `document`, tách biệt hoàn
toàn khỏi logic module:

```javascript
document.addEventListener('app:next',    () => nextStep());
document.addEventListener('app:prev',    () => prevStep());
document.addEventListener('app:back',     () => back());
document.addEventListener('app:shuffle',  () => shuffleStep());
```

- Nguồn: Keyboard / Mouse / Wheel(throttle 250ms) / Touch swipe / Gamepad.
- Phím: →↓Enter PgDn = NEXT; ←↑ PgUp = PREV; Esc/Bksp = BACK; Space = SHUFFLE;
  keyCode 123 (F12) = NEXT.
- Bỏ qua input khi focus trong `INPUT/TEXTAREA/SELECT/BUTTON/.exam-modal`
  hoặc khi `isSuspended=true` (module có custom controls tự gọi `suspend()`).
- `throttle(fn, cooldown)`: trailing+leading edge.

## 6. HIỆU CHUẨN QUY MÔ VẬT LÝ (Calibration)

### 6.1 Công thức kích thước optotype (js/calibration.js)
```javascript
// h_mm = D_mm * tan(5' * 10^logmar);  px = h_mm * (ppi / 25.4)
arcminutes = 5 * Math.pow(10, logmar);
radians    = (arcminutes / 60) * (Math.PI / 180);
heightMm   = (distanceM * 1000) * Math.tan(radians);
heightPx   = heightMm * (ppi / 25.4);   // số thực, KHÔNG round/clamp
getOptotypeSize(logmarValue, calib={distanceM, ppi}) -> heightPx
```
- `DisplayCalibrator` lưu `distanceM` (mặc định 4m; near 0.4m).
- **Ưu tiên nguồn PPI** (quan trọng nhất cho độ chính xác lâm sàng):
  1. Credit-card calibration: `localStorage['vision-therapy-cc-pxpermm']`
     → `ppi = pxPerMm * 25.4`
  2. `window.__calibrator.pxPerMm` (DisplayCalibrator)
  3. Fallback ước lượng: `diagPx / 24 inch` (không chính xác)
- `CreditCardCalibrator`: thẻ chuẩn 85.6 × 53.98 mm. Kéo slider khớp
  mép thẻ → `pxPerMm = sliderPixels / 85.6`, `ppi = pxPerMm * 25.4`.
  Lưu vào `localStorage['vision-therapy-cc-pxpermm']`. Cảnh báo Zoom 100%.

### 6.2 Pygame calibration (neuro_ophthalmology/calibration.py)
```python
PPD = d_mm * tan(pi/180) * R / W        # pixels per degree
stripe_width_px = PPD / (2 * cpd)        # 1 cycle = 2 stripes
velocity_px_s   = PPD * ang_vel_deg_s
```
- `ScreenInfo`: lấy `res_w/res_h` từ pygame, tính `physical_width_mm`,
  `ppi`, `ppd`, `px_per_mm`. `set_distance(mm)`, `set_diagonal(inches)`.

## 7. WORKSPACE & SỰ KIỆN TOÀN CỤC

- Hai workspace: `diagnostic` (#workspace-diagnostic / #menu-diagnostic)
  và `therapeutic` (#workspace-therapeutic / #menu-therapeutic).
- `toggleWorkspace()` (js/main.js:1044): ẩn/hiện DOM + dispatch
  `CustomEvent('onWorkspaceChanged', {fromWorkspace, toWorkspace})`
  để module dọn dẹp (vd dừng game).
- `therapeutic-content`: vùng mount game huấn luyện.
- Nút toggle: `#workspace-toggle-btn` (header sidebar, icon gamepad/clinic).

## 8. HỢP ĐỒNG GAME HUẤN LUYỆN (THERAPEUTIC) — 13 GAME M1..M13

Game kế thừa `BinocularGameEngine` (modules/binocular_game_engine.js):
- `this.gameName` (vd `'M1: Hứng hạt (Anti-suppression)'`).
- `super()` kiểm tra `window.__anaglyphColors`, tạo canvas, bind SPA.
- Kết thúc: dispatch `CustomEvent('onTherapeuticSessionEnd', {detail:
  {timestamp, durationMs, metrics, opticalSettings}})`.
- `main.js` bắt sự kiện → build `therapyRecord`:
  `{id, timestamp, gameName, durationSeconds, metrics, opticalSettings}`
  → `examSessionManager.addTherapyRecord(record)`.
- `metrics.customData` chứa chỉ số lâm sàng: `finalAlpha` (M1 C-Ratio),
  `visualAngleDeg` (M2), `avgBaseOut/avgBaseIn` (M3 BO/BI),
  `finalArcsec` (M5 stereo RDS), `avgLatencyMs` (M4, ngưỡng Fitts
  500ms cảm ứng / 900ms chuột), `accuracyRate/avgReactionTimeMs` (M7),
  `accuracy` (M8), `finalLogCS` (M11), `finalConvergenceDiopter` (M13),
  `finalDivergenceDiopter` (M6).
- `formatTherapyClinicalResult` / `generateTherapyReportHTML` (main.js)
  parse `customData` → chuỗi kết quả + ĐẠT/CHƯA ĐẠT.
  ⚠️ Thứ tự kiểm tra `gameName.includes('Mx')` PHẢI đặt M13/M12 trước
  M3/M2/M1 (vì "M13" chứa chuỗi "M3").

### 8.1 Global Result Modal (thay alert() cũ)

- `index.html` có `#global-result-modal` (dark theme, z-index 999999).
- Chuỗi sự kiện kết thúc: game kết thúc →
  `therapy_session_completed` CustomEvent (dispatch bởi main.js sau khi
  lưu EMR thành công, HOẶC trực tiếp từ M6/M13) →
  `exam_session_manager.js` bắt event, format văn bản lâm sàng theo
  `detail.gameId` (M3/M6/M13 có định dạng riêng; game khác hiện
  `detail.score`) → hiện modal → nút "Xác nhận & Lưu hồ sơ" gọi
  `closeGlobalResultModal()` → `window.closeTherapyModule()` về Lobby.
- Modal chống trùng lặp: bỏ qua nếu `modal.style.display === 'flex'`.
- M3/M6/M13 KHÔNG còn tự tạo overlay kết quả riêng — nhường cho modal
  toàn cục. Payload chuẩn hóa: `{gameId, gameName, duration, score,
  metrics}`.

## 9. DATA MODEL & LOCALSTORAGE KEYS

| Key | Nội dung |
|-----|----------|
| `vision_therapy_active_session` | phiên khám đang mở (auto-save) |
| `vision_emr_history_v1` | lịch sử bệnh án (≤200 bản ghi) |
| `emr_patient_sessions` | sessions theo patientId, chứa `therapy_records[]` |
| `vision_clinic_settings` | tên PK, bác sĩ, logo, địa chỉ |
| `vision_color_calibration` | `window.__anaglyphColors` (red/cyan) → CSS `--calibrated-red/cyan` |
| `vision-therapy-calibrate-distance-m` | khoảng cách khám (m) |
| `vision-therapy-cc-pxpermm` | px/mm từ thẻ tín dụng (nguồn PPI chuẩn) |
| `currentPatientId` | 🔥 ID bệnh nhân đang khám (`SĐT_tênchuẩnhóa_namsinh`) — cần cho Dashboard |
| `currentPatientName` | 🔥 tên hiển thị bệnh nhân hiện tại |
| `currentPatientYob` | 🔥 năm sinh bệnh nhân — điền sẵn lại ô Năm sinh khi mở modal Bắt đầu khám |
| `currentProtocol` | 🔥 phác đồ điều trị: `amblyopia` | `strabismus_postop` |

### 9.1 Firestore Data Model (🔥 MỚI)

```text
Patients/{patientId}                          // patientId = phone_normalizedName_birthYear
  ├─ phone, fullName, birthYear, status: "Active",
  │  protocol ("amblyopia" | "strabismus_postop"),
  │  createdAt: serverTimestamp()
  └─ Sessions/ (auto-ID)                       // 1 doc / 1 phiên tập
       ├─ gameName (vd "M13: Tập Hội Tụ")
       ├─ durationSeconds
       ├─ metrics: {customData của game}       // VD finalConvergenceDiopter, avgBaseOut...
       ├─ opticalSettings
       ├─ timestamp: serverTimestamp()
       └─ device_userAgent
```

- Ghi: `examSessionManager.addTherapyRecord()` — sau khi hard-write
  localStorage, nếu có `currentPatientId` + `window.db` thì `sessionsRef.add(payload)`.
- Đọc: `dashboard_controller.js` — `Patients/{id}/Sessions`
  `orderBy("timestamp","asc")`, lọc `data.timestamp && data.metrics`,
  chỉ giữ series ≥ 2 phiên.
- Lưu ý: Firestore cần `@firebase/firestore` compat (CDN) — nếu offline,
  app vẫn chạy bình thường trên localStorage (offline-first).

### 9.2 Đăng ký bệnh nhân (Blind Authentication)

- Form "Bắt đầu khám" giờ có thêm: SĐT (`#input_phone`), Phác đồ
  (`#input_protocol`: Nhược thị / Hậu phẫu Lác), checkbox Khám ẩn danh
  (`#anonymous-check` → sinh ID `ANON_xxxxxx`).
- `handleSessionStart()` (exam_session_manager.js): normalize tên
  (bỏ dấu, lowercase, `đ→d`, bỏ khoảng trắng) → `patientId =
  phone_normalizedName_yob` → `patientRef.get()`: nếu có → mở phiên;
  không → confirm rồi `patientRef.set(...)` tạo hồ sơ mới.
- Lỗi Firebase → alert "Không thể kết nối máy chủ", chặn mở phiên.

- Báo cáo: `generateReportHTML`, `exportPDF` (html2pdf.js),
  `exportHistoryToCSV` (UTF-8 BOM). Manual entry: `createManualEntryModal`
  với `TEST_NAMES_LIST` (~30 tên test chuẩn).
- Global: `window.__state`, `window.__getTestModule`, `window.__calibrator`,
  `window.__ccCal`, `window.__displayManager`, `window.__anaglyphColors`,
  `window.__currentExam`, `window.examSessionManager`.

## 10. DOM IDs THỰC TẾ (index.html)

Tĩnh: `app, sidebar, sidebar-header, settings-btn, cc-calib-btn,
fullscreen-btn, sidebar-title, workspace-toggle-btn, menu-diagnostic,
menu-therapeutic, menu-toggle-btn, menu-hotzone, main-area,
workspace-diagnostic, display-board, workspace-therapeutic,
therapeutic-content`.

Động (tạo bởi exam_session_manager.js): `start-exam-btn, end-exam-btn,
exam-status-text, start-exam-modal, end-exam-modal, report-modal,
manual-entry-modal, history-modal, exam-session-container,
manual-save-fab, toast-container`. Menu item: `.menu-item[data-test=...]`.

## 11. ĐIỀU HƯỚNG BÀN PHÍM (main.js)

- Modal active (`.exam-modal/.settings-modal-overlay/.calib-modal-overlay`):
  trap focus, 2D spatial nav (`findClosestElementInDirection`).
- Text input: ArrowUp/Down chuyển item, Left/Right để caret.
- Toggle menu: `` ` `` / `~` / `Home` / `ContextMenu` / mouse btn 4-5 /
  hover góc dưới-trái 120×120px.
- `MutationObserver` auto-focus ô nhập đầu tiên khi modal mở; lưu
  `lastFocusedElementBeforeModal` để restore focus khi đóng.
- A11y (🔥 MỚI): nút "Bắt đầu khám"/"Hủy" là `<button>` chuẩn, KHÔNG có
  tabindex dương (trước đây `tabindex="4"/"5"` làm Tab không tới được);
  Tab chạy theo DOM: inputs → checkbox → Bắt đầu khám → Hủy. Enter kích
  hoạt native (submit form / click). Viền focus:
  `.submit-btn:focus-visible, .cancel-btn:focus-visible` (outline #0056b3)
  trong css/exam_session.css.

## 12. LƯU Ý THIẾT KẾ QUAN TRỌNG

- Độ chính xác lâm sàng phụ thuộc PPI từ credit-card calibration → ưu
  tiên bảo vệ luồng này khi sửa đổi.
- Mọi module test PHẢI render vào `#display-board`, không tự gắn listener
  xung đột với UniversalInput (dùng `suspend()` nếu cần custom controls).
- Python Pygame app là tool riêng; nếu muốn đồng bộ calibration cần
  cơ chế trao đổi (file/json) — hiện chưa có.
- Codebase dùng Tiếng Việt trong comment/log; giữ nhất quán khi mở rộng.

---

# =================================================================
# PHẦN MỞ RỘNG 2 — SƠ ĐỒ LUỒNG, DANH MỤC & PROMPT GEMINI
# =================================================================

## 13. SƠ ĐỒ LUỒNG DỮ LIỆU (ASCII)

### 13.1 Khởi động (Boot)
```
index.html
   │
   └─<script type="module" src="js/main.js">
         │
         ▼
   main.js :: init()
         ├─ setupSidebar()        → gắn click .menu-item, điều hướng D-Pad
         ├─ setupInput()          → new UniversalInput() → document 'app:*'
         ├─ setupDisplay()        → DisplayManager (#app, autoApply)
         ├─ setupCalibrator()     → DisplayCalibrator + CreditCardCalibrator
         │                          → window.__calibrator / window.__ccCal
         ├─ setupWorkspaceToggle()→ #workspace-toggle-btn
         └─ loadTest(state.currentTest, mod.steps) → renderStep()
```

### 13.2 Hiệu chuẩn (Calibration pipeline)
```
CreditCardCalibrator.showModal()
   └─ user kéo slider khớp thẻ 85.6mm
         └─ _saveAndSync(px) → localStorage['vision-therapy-cc-pxpermm']
               └─ DisplayCalibrator._recalculate()
                     pxPerMm = savedPx/85.6 ; ppi = pxPerMm*25.4
                           │
                           ▼
   getOptotypeSize(logmar, {distanceM, ppi})
       = (distanceM*1000)*tan((5*10^logmar/60)*π/180) * (ppi/25.4)
                           │
                           ▼
   module.render(index) → #display-board (SVG/Canvas kích thước px chuẩn)
```

### 13.3 Bài test (Diagnostic flow)
```
[Sidebar .menu-item click]
   → loadTest(testId, mod.steps)
        → prevMod.cleanup()  (dọn loop nếu có)
        → UniversalInput.resume()
        → state.history.push(...) ; state.stepIndex=0
        → renderStep() → mod.render(idx) vào #display-board
   [UniversalInput CustomEvent]
        app:next  → nextStep()  (idx++)
        app:prev  → prevStep()  (idx--)
        app:back  → back()      (pop history)
        app:shuffle → mod.randomize() (giữ LogMAR)
   [Sidebar near-*/far-*] → auto applyNearVisionPreset()/applyDistanceVisionPreset()
```

### 13.4 Huấn luyện (Therapeutic flow)
```
#workspace-toggle-btn → toggleWorkspace() → 'therapeutic'
   → onWorkspaceChanged event → TherapeuticMenuController.init()
   → [Chọn game M1..M6] → _renderLobby() (hướng dẫn lâm sàng)
        → btn-start-fullscreen → _startFullscreenGame()
             → new module.classRef() ; game.start(config)
                  │ (game kế thừa BinocularGameEngine, dùng window.__anaglyphColors)
                  ▼
             game kết thúc → dispatch 'onTherapeuticSessionEnd'
                  {timestamp, durationMs, metrics, opticalSettings}
                  │
                  ▼
             main.js → build therapyRecord
                  → examSessionManager.addTherapyRecord(record)
                  → localStorage['emr_patient_sessions']
   [Phác đồ tự động] CatchGame→'requestLaunchModule2'→M2; M2→M3; RDS→M6
```

### 13.5 Lưu trữ EMR
```
Bắt đầu khám → createStartExamModal → startExam(name, YOB, patientId)
   → window.__currentExam ; auto-save 'vision_therapy_active_session'
Kết thúc    → createEndExamModal → saveSession() / saveToHistory()
   → 'vision_emr_history_v1' (≤200)
Xuất        → exportPDF (html2pdf.js) / exportHistoryToCSV (UTF-8 BOM)
Clinic      → 'vision_clinic_settings' (tên PK, BS, logo)
Anaglyph    → 'vision_color_calibration' → --calibrated-red/cyan
```

## 14. DANH MỤC ĐẦY ĐỦ BÀI TEST & GAME (30+ test + 13 game)

### 14.1 Bài test chẩn đoán (Diagnostic) — file module tương ứng
| ID (data-test) | Tên | Module file | Nhóm |
|----------------|-----|-------------|------|
| far-vision / far-vision-etdrs | ETDRS Chart | etdrs_chart.js | Far |
| far-vision-snellen | Snellen Chart | snellen_chart.js | Far |
| far-vision-lea | LEA Symbols | lea_symbols.js | Far |
| far-vision-landolt | Landolt C | landolt_c.js | Far |
| far-vision-tumbling-e | Tumbling E | tumbling_e.js | Far |
| far-vision-auto-bcva-crowding | Auto BCVA (Crowding/Amblyopia) | auto_BCVA_tumbling_e_for_amblyopia.js | Far |
| far-vision-numbers | Number Chart 0–9 | number_chart.js | Far |
| far-vision-hotv | HOTV Letters | hotv.js | Far |
| far-vision-auckland | Auckland LogMAR | auckland_logmar.js | Far |
| (distance_va) | Distance VA | distance_va.js | Far |
| near-vision-logmar | Near LogMAR 40cm | near_logmar.js | Near |
| near-vision-npoint | Near N-Point 40cm | near_npoint.js | Near |
| near-vision-lea | Near LEA Symbols | near_lea.js | Near |
| retina-amsler | Amsler Grid | retina_amsler.js | Retina |
| retina-ishihara | Ishihara Color Test | retina_ishihara.js | Retina |
| retina-pelli-robson | Pelli-Robson Contrast | retina_pelli_robson.js | Retina |
| (retina_subs) | Sloan/Grayscale helper | retina_subs.js | Retina |
| binocular | Worth 4 Dot | worth4dot.js | Binocular |
| binocular-stereo | Stereo Anaglyph (RDS/WebGL) | stereo_anaglyph.js | Binocular |
| schober-heterophoria | Schober Heterophoria | schober_test.js | Binocular |
| maddox-grid | Maddox Grid (Heterophoria) | maddox_grid_module.js | Binocular |
| (crosstalk) | Crosstalk 3D cancellation | crosstalk.js | Binocular |
| dynamic-vergence | Dynamic Vergence | dynamic_vergence.js | Binocular |
| astigmatism | Astigmatism (dial/Maddox) | astigmatism.js | Refraction |
| jcc-simulation | JCC Simulation | jcc_simulation.js | Refraction |
| astigmatism_jcc | Astigmatism JCC | astigmatism_jcc.js | Refraction |
| neuro-duochrome | Duochrome Test | duochrome_test.js | Refraction |
| neuro-red-desat | Red Desaturation | red_desat.js | Neuro |
| neuro-okn | Neuro OKN | neuro_okn.js | Neuro |
| dynamic-fixation | Dynamic Fixation (Pediatric) | dynamic_fixation.js | Neuro |
| hiding-heidi | Hiding Heidi (Preferential Looking) | hiding_heidi.js | Neuro |
| accommodation | Điều tiết (fallback makeModule) | main.js | Other |

> Tổng: ~31 bài test/chẩn đoán. Helper dùng chung: optotype_paths.js,
> drthe_optotype_loader.js, therapeutic_menu_controller.js.

### 14.2 Game huấn luyện (Therapeutic) — M1..M13
| Module | Tên | classRef | File | Mục tiêu lâm sàng |
|--------|-----|----------|------|-------------------|
| M1 | Hứng hạt (Anti-suppression) | CatchGame | anti_suppression_catch.js | C-Ratio ≤ 0.5 (adaptive staircase) |
| M2 | Khớp khung (Flat Fusion) | ShapeAlignmentGame | shape_alignment.js | Foveal angle ≤ 2.0°, 10 cấp |
| M3 | Vận nhãn (Vergence) | VergenceTrackerGame | vergence_tracker_game.js | PFV BO ≥ 15Δ, NFV BI ≥ 8Δ @50cm |
| M4 | Vận nhãn nhanh (Saccadic) | SaccadicTrackingGame | saccadic_tracking_game.js | Latency ≤ 500ms (touch)/900ms (mouse) |
| M5 | Thị giác nổi (RDS) | RDSTherapyGame | rds_therapy_game.js | Stereoacuity ≤ 40 Arcsec |
| M6 | Phân kỳ (Divergence) | DivergenceTherapyGame | divergence_therapy_game.js | NFV 2Δ→8Δ, metric `finalDivergenceDiopter` |
| M7 | Kích thích Lưới quay CAM | CamVisualStimulatorGame | cam_visual_stimulator_game.js | Đơn thị, accuracy ≥ 80%, RT ≤ 800ms |
| M8 | Phá ức chế vùng cận (Anti-Crowding) | AntiCrowdingGame | anti_crowding_game.js | Tăng accuracy khi giảm crowding spacing |
| M9 | Kích thích tế bào nón Đỏ | RedConeStimulatorGame | red_cone_stimulator_game.js | Pleoptics đơn thị (Brinker-Katz) |
| M10 | OKN Tracker | OKNStimulationGame | okn_stimulation_game.js | Kích thích OKN đơn thị (monocular) |
| M11 | Gabor Perceptual Learning | GaborPerceptualLearningGame | gabor_perceptual_learning_game.js | LogCS (staircase 2 pha: giảm nhanh C×0.5 → 3-down/1-up C×0.9) |
| M12 | Bám đuôi (Pursuit) | DichopticPursuitGame | dichoptic_pursuit_game.js | Tracking accuracy, vận động mắt |
| M13 | Mở rộng Hội tụ (Convergence) | ConvergenceTherapyGame | convergence_therapy_game.js | PFV ≥ 15Δ (tăng 2Δ mỗi 10s giữ vững), metric `finalConvergenceDiopter` |
| — | Base engine | BinocularGameEngine | binocular_game_engine.js | Lớp cha chung (canvas, anaglyph, `finishSession`) |

> Phác đồ tự động: M1→M2→M3 và M5→M6 (qua CustomEvent
> `requestLaunchModule2/3/6`). M13 (chuyên biệt) độc lập, do bác sĩ chọn.
> Bảng đầy đủ mô tả lâm sàng từng game: thư mục `docs/` (§18).

### 14.3 Công cụ Python (Pygame — độc lập, không giao tiếp JS)
- main.py: init_pygame, main menu.
- calibration.py: PPD/stripe/velocity math (§6.2).
- ui.py: InputField, Button, MainMenu.
- red_desat.py: Red Desaturation app (hsv→rgb).
- okn_test.py: Optokinetic Nystagmus (Direction, OKNTest).
- warning_dialog.py: WarningDialog.

## 15. PROMPT TEMPLATE GỬI GEMINI (Cố vấn thiết kế)

Copy đoạn dưới đây, đính kèm file `gemini_context.txt` làm tài liệu tham chiếu.

```
Bạn là CỐ VẤN THIẾT KẾ PHẦN MỀM cho một dự án webapp nhãn khoa
(Vision Therapy / Mini-EMR). Tôi đã đính kèm file `gemini_context.txt`
mô tả kiến trúc, hợp đồng module, luồng dữ liệu và danh mục 30+ bài
test + 13 game huấn luyện (M1..M13). Hãy DỰA VÀO tài liệu đó (không tự
bịa cấu trúc) khi tư vấn.

QUY TẮC BẮT BUỘC:
1. Giữ nguyên stack: Vanilla JS ES6 Modules + Canvas/WebGL/SVG,
   localStorage offline. KHÔNG đề xuất React/Vue/build tool trừ khi
   tôi yêu cầu.
2. Mọi bài test mới PHẢI tuân thủ hợp đồng module (§4): object có
   {id, label, steps, render(index), cleanup?(), randomize?()}, render
   vào #display-board, dùng getOptotypeSize() cho kích thước chuẩn.
3. Mọi game mới PHẢI kế thừa BinocularGameEngine, dispatch
   'onTherapeuticSessionEnd' với metrics.customData (§8). Kết quả
   hiển thị qua Global Result Modal (§8.1) — KHÔNG tạo overlay riêng.
4. Không phá luồng hiệu chuẩn thẻ tín dụng (nguồn PPI chuẩn, §6).
5. Dùng Tiếng Việt trong comment/log nhất quán với codebase.
6. Trước khi đề xuất, hãy trích dẫn ID/module liên quan trong tài liệu.
7. Nếu tính năng liên quan dữ liệu bệnh nhân: tuân thủ Firestore model
   (§9.1) và offline-first (localStorage vẫn hoạt động khi mất mạng).

ĐỊNH DẠNG PHẢN HỒI:
- Tóm tắt yêu cầu (1–2 câu)
- Ảnh hưởng đến module nào (theo §14)
- Thiết kế đề xuất (interface/function signatures, luồng)
- Rủi ro lâm sàng / hiệu chuẩn cần lưu ý
- Code mẫu (chỉ phần cốt lõi)

YÊU CẦU HIỆN TẠI CỦA TÔI:
<MÔ TẢ TÍNH NĂNG CẦN THIẾT KẾ — ĐIỀN VÀO ĐÂY>
```

### 15.1 Ví dụ ask mẫu (điền vào cuối prompt)
- "Thiết kế bài test 'Far Vision Contrast Sensitivity' mới dùng
  logMAR + cờ crowding, tái dùng etdrs_chart."
- "Thêm game M7 'Pursuit Tracking' kế thừa BinocularGameEngine, đo
  velocity theo §6.2."
- "Refactor: gom 9 bài far-vision thành 1 module cấu hình được, giữ
  data-test cũ để không gãy menu."

---

# =================================================================
# PHẦN MỞ RỘNG 3 — CẬP NHẬT 30/08/2026: FIREBASE, DASHBOARD,
# GLOBAL RESULT MODAL, LOBY THEO GIAI ĐOẠN, DOCS HUB
# =================================================================

## 16. DASHBOARD TIẾN TRIỂN (js/dashboard_controller.js)

- Nút 📊 (`#top-dashboard-btn`, tạo động, chèn cạnh `#fullscreen-btn`):
  chỉ mở khi có `localStorage.currentPatientId` (đã "Bắt đầu khám").
- `window.openDashboard()`: hiện `#progress-dashboard-modal` →
  `fetchFirebaseData()` → `buildDropdown()` → `renderChart()`.
- `fetchFirebaseData()`: đọc `Patients/{id}/Sessions` (asc theo
  timestamp); build `dynamicSeries` theo khóa `{gameName}_{metricKey}`;
  chỉ giữ series ≥ 2 phiên. Mỗi series: `{gameName, metricKey, labels[],
  dataPoints[]}` (nhãn = ngày vi-VN).
- `METRIC_LABELS`: map chỉ số lâm sàng → tên Tiếng Việt (vd
  `finalConvergenceDiopter → 'Dự trữ Hợp thị Hội tụ PFV (Δ)'`).
  Thêm chỉ số mới → PHẢI thêm nhãn vào đây.
- Chart: Chart.js (CDN) line chart, y `beginAtZero: false` (dao động
  ±15% là sinh lý bình thường — ghi chú hiển thị trong modal).
- Không có patientId / chưa đủ 2 phiên → dropdown thông báo rỗng.

## 17. PHÁC ĐỒ ĐIỀU TRỊ (Protocols)

- `#input_protocol` trong form Bắt đầu khám:
  - `amblyopia` — Nhược thị (mặc định)
  - `strabismus_postop` — Hậu phẫu Lác (không bịt mắt)
- Lưu vào `localStorage.currentProtocol` + field `protocol` trong doc
  `Patients/{patientId}`.
- Tài liệu lâm sàng chi tiết: `docs/post_op_strabismus_protocol.html`
  (hướng dẫn vận hành, tiêu chí theo dõi hậu phẫu).

## 18. DOCS HUB & LOBY GAME THEO PHÁC ĐỒ

### 18.1 Trung tâm tài liệu (docs/)
- `docs/index.html` + `docs/css/docs.css`: hub tài liệu y khoa.
- `docs/m01_catch.html` … `docs/m12_pursuit.html`: 12 trang, mỗi trang
  mô tả 1 module: chỉ định, chống chỉ định, hướng dẫn vận hành,
  tiêu chí đạt/không đạt. Lưu ý: CHƯA có trang m13_convergence.html.
- `docs/post_op_strabismus_protocol.html`: sổ tay phác đồ hậu phẫu lác.
- `docs/index.html` có nhóm nav "📋 Sổ tay vận hành" chứa link phác đồ.

### 18.2 Loby game theo PHÁC ĐỒ (therapeutic_menu_controller.js)
- `this._moduleIdByM`: map `M1..M13 → id` module game — nguồn chuẩn
  duy nhất cho ánh xạ mã hiệu y khoa → module id.
- `renderSidebar()` → `window.renderTherapeuticLobby(container)`:
  render sảnh dạng **CSS Grid** động theo `localStorage.currentProtocol`:
  - `amblyopia` — Phác đồ Nhược thị, 4 giai đoạn:
    1. Đánh thức Hoàng điểm (Đơn thị): M7, M8, M9, M10, M11
    2. Chống ức chế: M1, M2, M12
    3. Hợp thị Vận động: M3, M6, M13
    4. Thị giác nổi 3D: M5
  - `strabismus_postop` — Phác đồ Hậu phẫu Lác (không bịt mắt), 3 bước:
    1. Phá vỡ ức chế + Hợp thị cảm giác: M1, M2, M12
    2. Hợp thị Vận động: M3, M6, M13
    3. Keo 3D: M5
  - Mỗi hàng: cột trái thẻ Giai đoạn (220px) + tối đa 5 module card.
  - CHƯA hiệu chuẩn kính (`!window.__anaglyphColors.red`) → card
    mờ + tooltip "Chống chỉ định: Cần hiệu chuẩn kính" (bắt buộc).
- `window.startTherapyModule('Mx')` → `_getModuleByMId()` →
  `launchGame(mod)`; `window.closeTherapyModule()` → dọn game +
  `loadTest('dashboard')`.
- `_docHrefFor(moduleId)`: map module id → trang docs tương ứng
  (`/docs/m11_gabor.html`...); module mới PHẢI thêm vào docMap.
- `_renderLobby(module)`: lobby hướng dẫn lâm sàng trước khi chơi
  (parentTranslation, medicalPurpose, indication, contraindication,
  gameplay, goal, settings[], mandatoryWarning).
- `launchGame(module)`: `new classRef()` + `game.start(config)` từ
  settings đã chọn; `_handleFullscreenExit` (thoát fullscreen →
  `stopCurrentGame()` + dọn workspace).

## 19. CHUỖI SỰ KIỆN KẾT THÚC BÀI TẬP (Toàn cảnh)

```
Game (M1..M12, qua BinocularGameEngine)          M6/M13 (dispatch trực tiếp)
   │ _endGame(): sessionMetrics.customData=...        │
   │ finishSession() → 'onTherapeuticSessionEnd'      │ 'therapy_session_completed'
   ▼                                                   ▼
main.js listener (l.1163)                     exam_session_manager.js listener
   │ build therapyRecord →                         │ (l.2821)
   │ examSessionManager.addTherapyRecord(record)   │ format theo gameId
   ▼                                               │ (M3/M6/M13 đặc thù, khác = score)
exam_session_manager.js addTherapyRecord()         ▼
   │ 1. localStorage: emr_patient_sessions    #global-result-modal hiện
   │ 2. Firestore: Patients/{id}/Sessions.add()    (chống trùng qua display='flex')
   └─> dispatch 'therapy_session_completed'  ◄── nút Xác nhận →
       {gameId, gameName, duration, score,          closeGlobalResultModal()
        metrics: customData}                        → window.closeTherapyModule() → Lobby
```

## 20. TỔNG KẾT THAY ĐỔI TỪ 28/08 → 30/08/2026

| Khu vực | Thay đổi |
|---------|----------|
| Game | +M7 CAM, M8 Anti-Crowding, M9 RED-Cone, M10 OKN, M11 Gabor (LogCS), M12 Pursuit, M13 Convergence (tổng 13) |
| UI kết quả | Bỏ overlay/alert riêng từng game → Global Result Modal tập trung |
| EMR | `formatTherapyClinicalResult` hỗ trợ M6/M7/M13 (Δ, accuracy, LogCS); M3/M6/M13: score = Δ |
| Backend | Firebase Firestore: Patients + Sessions (song song localStorage) |
| Dashboard | Chart.js biểu đồ tiến triển theo phiên, nút 📊 |
| Đăng ký BN | +SĐT, +Phác đồ, +Khám ẩn danh, ID chuẩn hóa `SĐT_tên_yob` |
| Docs | Trung tâm tài liệu 12 module + phác đồ hậu phẫu lác |
| Menu | Loby game chia theo Phác đồ (amblyopia 4 giai đoạn / strabismus_postop 3 bước), map `_moduleIdByM` |


## 21. THAY ĐỔI 03/09/2026 (commit 64b36e0)

| Khu vực | Thay đổi |
|---------|----------|
| Patient ID | Hợp nhất định danh chuẩn `[SĐT]_[tên đã chuẩn hóa]_[năm sinh]` qua `buildPatientId()` (bỏ dấu, lowercase, `đ→d`, bỏ khoảng trắng). `window.__currentExam.patientId` KHÔNG còn timestamp → cùng bệnh nhân nhập trùng SĐT+Tên+Năm sinh luôn quy về cùng ID (Firestore `Patients/<id>`, `currentPatientId`, `emr_patient_sessions`), chống phân mảnh EMR. Timestamp chỉ lưu ở `startTime`. |
| Debug logs | Xóa toàn bộ `console.log` trong `js/` + `modules/` (~85 dòng, 16 file), xóa hàm debug `debugPrintSizes()` (calibration.js) + block log `_emit` (controller.js) + `DisplayManager` log (settings.js). Chỉ giữ `console.error`/`console.warn`. |
| Placeholder | Xóa `neuroRedDesatModule` giả lập "đang được phát triển" (neuro_okn.js) — module thật `red_desat.js` đã đăng ký cùng id; xóa block "đang được phát triển..." trong `#menu-therapeutic` (index.html). |
| Fix sidebar | `refreshTherapeuticMenu()` + `renderTherapeuticLobby()` giờ ẩn `#menu-diagnostic` (display:none) khi bật menu Huấn luyện — trước đây sau đăng nhập cả 2 menu cùng hiển thị xếp dọc chia đôi sidebar. |
| ESM | `export { DisplayCalibrator, getOptotypeSize }` (bỏ `debugPrintSizes`); `neuro_okn.js` bỏ export `neuroRedDesatModule`. |

## 22. PWA + A11Y + UX 05/09/2026 (commit 0ec5160)

### 22.1 PWA — Hoạt động ngoại tuyến (Service Worker `sw.js`)

- **Chiến lược**: Network First, Fallback to Cache — luôn lấy code mới nhất
  từ mạng; khi mất mạng `.catch()` → `caches.match(event.request)`; nếu
  `request.mode === 'navigate'` và URL chưa cache → trả `/offline.html`.
- **Pre-cache (install)**: `CACHE_NAME = 'vision-emr-cache-v1'`, `urlsToCache`
  = toàn bộ App Shell: `/`, `/index.html`, `/offline.html`, 4 CSS,
  8 file `js/`, toàn bộ module graph (Khám + Huấn luyện + `auto_*` +
  engine phụ trợ) và 5 thư viện CDN (FontAwesome, Firebase 9.23.0 compat,
  Chart.js, html2pdf). `cache.addAll()` có fallback cache từng file
  (`Promise.allSettled`) để đường dẫn lỗi không làm hỏng cài đặt.
- **Cập nhật có cấp phép (chống mất session)**: install KHÔNG tự động
  `skipWaiting()`. Trang gửi `postMessage({type:'SKIP_WAITING'})` → worker
  cũ `message` listener → `skipWaiting()` → activate → `clients.claim()` →
  `controllerchange` → reload.
- **activate**: xóa cache cũ không khớp `CACHE_NAME` (giữ `clients.claim()`).
- **Manifest**: `manifest.json` — `start_url:"/"`, `display:"standalone"`,
  `theme_color:#1a1a2e`, icons `icons/icon-{192,512}.png` (placeholder
  sinh bằng script Node, PNG hợp lệ).

### 22.2 UI trạng thái mạng & cập nhật (js/main.js — IIFE `initPWA` cuối file)

- Toast "Hệ thống đã sẵn sàng hoạt động ngoại tuyến" (3s) khi
  `register('/sw.js')` resolve.
- `reg.onupdatefound` + `statechange` → worker `installed` VÀ đã có
  `navigator.serviceWorker.controller` cũ → Toast "Có phiên bản mới.
  [Bấm để tải lại]" (duration 0, chờ người dùng); nút bấm →
  `postMessage({type:'SKIP_WAITING'})`. `controllerchange` → reload.
- Badge đỏ "Ngoại tuyến - Dữ liệu lưu nội bộ" (`#pwa-offline-badge`, góc
  trên giữa) qua `window 'offline'` / `'online'` events; khởi tạo theo
  `navigator.onLine`; ẩn tự động khi có mạng.
- Toast PWA dùng chung class `.toast-container`/`.toast-notification`
  (exam_session.css), container riêng z-index 2147483000.

### 22.3 A11y — Popup "Bắt đầu phiên khám"

- Bỏ `tabindex="4"`/`tabindex="5"` trên nút "Bắt đầu khám"/"Hủy"
  (exam_session_manager.js): trước đây positive tabindex xếp chúng vào
  nhóm Tab riêng → Tab thuận từ form không tới được (vòng lại từ nút
  đóng). Nay Tab chạy đúng DOM order: inputs → checkbox Ẩn danh →
  Bắt đầu khám → Hủy. Enter hoạt động native (`<button>` + form submit).
- CSS: `.submit-btn:focus-visible, .cancel-btn:focus-visible` →
  `outline: 2px solid #0056b3; outline-offset: 2px` + box-shadow mềm
  (exam_session.css).

### 22.4 UX — Nhớ Năm sinh người bệnh

- `handleSessionStart()` lưu thêm `localStorage.currentPatientYob` (cả
  nhánh hồ sơ cũ + mới); `prefillStartExamModal()` điền sẵn vào
  `#patient-yob` khi mở modal (chỉ điền khi ô trống, bỏ qua nếu disabled
  do Khám ẩn danh) — ngang bằng Họ tên/SĐT.

### 22.5 Đổi tên phác đồ

- Nhãn `amblyopia`: "Nhược thị & Tật khúc xạ" → **"Nhược thị"** tại
  option `#input_protocol` (exam_session_manager.js) + tiêu đề Lobby
  "Phác đồ Điều trị Nhược thị" (therapeutic_menu_controller.js).
  Giá trị lưu trữ `amblyopia` KHÔNG đổi (tương thích dữ liệu cũ).
